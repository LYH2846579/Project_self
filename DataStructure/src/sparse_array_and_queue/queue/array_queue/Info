以数组的形式实现队列

1.使用front和rear指针确定首位位置

2.front == rear      -> 队列空
  rear = maxSize-1  -> 队列即将满    ->  还允许添加一个

3.如何实现队列的循环利用?
  解决：将front指针不断指向数组的第0位，当存在元素出队时，将所有元素前移

4.
优点：可以使得队列实现重复利用
缺点：当数组较大的时候，若出现出队列的情况，需要移动大批元素 -> 尝试使用循环队列解决


5.在ArrayQueuePlus中使用环形队列
  扩展：① 增加判断队列空与队列满的机制

  问题分析：会留有一个空余